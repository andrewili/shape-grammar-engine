#   sg_labeled_point_test.txt
#   2013-10-04. Extends 2013-09-24


    >>> import sg_labeled_point
    >>> def make_lpoint_from_spec(x, y, label):
    ...     return sg_labeled_point.SGLabeledPoint(x, y, label)

#### init ####
    
__init__()
    >>> lp1 = make_lpoint_from_spec(1, 2, 'a')
    
    >>> lp1.spec
    (1, 2, 'a')
    >>> lp1.point.spec
    (1, 2)
    >>> lp1.x
    1
    >>> lp1.y
    2
    >>> lp1.label
    'a'
    
    >>> lp2 = make_lpoint_from_spec(1, 2, '')
    You're trying to create a labeled point with an empty label

#### relations ####
    
__eq__(other)
    >>> lp =  make_lpoint_from_spec(0, 0, 'o')
    
    >>> lp == make_lpoint_from_spec(0, 0, 'o')
    True
    >>> lp == make_lpoint_from_spec(1, 0, 'o')
    False
    >>> lp == make_lpoint_from_spec(0, 1, 'o')
    False
    >>> lp == make_lpoint_from_spec(0, 0, 'x')
    False

__ge__(other)
    >>> lp  = make_lpoint_from_spec( 0,  0, 'o')
    
    >>> lp >= make_lpoint_from_spec(-1, -1, 'z')
    True
    >>> lp >= make_lpoint_from_spec( 0,  0, 'n')
    True
    >>> lp >= make_lpoint_from_spec( 0,  0, 'o')
    True
    >>> lp >= make_lpoint_from_spec( 0,  0, 'z')
    False
    >>> lp >= make_lpoint_from_spec( 2,  2, 'z')
    False

__gt__(other)
    >>> lp = make_lpoint_from_spec( 0,  0, 'o')
    
    >>> lp > make_lpoint_from_spec(-1, -1, 'z')
    True
    >>> lp > make_lpoint_from_spec( 0,  0, 'n')
    True
    >>> lp > make_lpoint_from_spec( 0,  0, 'o')
    False
    >>> lp > make_lpoint_from_spec( 0,  0, 'z')
    False
    >>> lp > make_lpoint_from_spec( 2,  2, 'z')
    False

__le__(other)
    >>> lp  =  make_lpoint_from_spec( 0,  0, 'o')
    
    >>> lp <=  make_lpoint_from_spec( 1,  1, 'a')
    True
    >>> lp <=  make_lpoint_from_spec( 0,  0, 'p')
    True
    >>> lp <=  make_lpoint_from_spec( 0,  0, 'o')
    True
    >>> lp <=  make_lpoint_from_spec( 0,  0, 'a')
    False
    >>> lp <=  make_lpoint_from_spec(-2, -2, 'a')
    False

__lt__(other)
    >>> lp = make_lpoint_from_spec( 0,  0, 'o')
    
    >>> lp < make_lpoint_from_spec( 1,  1, 'a')
    True
    >>> lp < make_lpoint_from_spec( 0,  0, 'p')
    True
    >>> lp < make_lpoint_from_spec( 0,  0, 'o')
    False
    >>> lp < make_lpoint_from_spec( 0,  0, 'a')
    False
    >>> lp < make_lpoint_from_spec(-2, -2, 'a')
    False


__ne__(other)
    >>> lp =  make_lpoint_from_spec(0, 0, 'o')
    
    >>> lp != make_lpoint_from_spec(0, 0, 'o')
    False
    >>> lp != make_lpoint_from_spec(1, 0, 'o')
    True
    >>> lp != make_lpoint_from_spec(0, 1, 'o')
    True
    >>> lp != make_lpoint_from_spec(0, 0, 'x')
    True

#### representation ####
    
__str__()
    >>> print make_lpoint_from_spec(1, 2, 'a')
    (1.0, 2.0, a)
    
