#   model_test.txt

    >>> import model

#	__init__()

    >>> model = model.Model()
    >>> model.lshape_a.is_empty()
    True
    >>> model.lshape_b.is_empty()
    True
    >>> model.lshape_c.is_empty()
    True

