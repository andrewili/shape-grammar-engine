#   element_cell_test.txt

    >>> import element_cell
    >>> import line

    ### construct
    #   __init__()

    bad input: lines and lpoints combined, elements other than lines and lpoints,
    no elements

    >>> def make_lines_for_init_test():
    ...     line_01 = line.Line.from_short_spec(0, 1)
    ...     line_23 = line.Line.from_short_spec(2, 3)
    ...     lines = [line_23, line_01]
    ...     return lines
    >>> lines = make_lines_for_init_test()

    >>> line_cell = element_cell.ElementCell([lines])