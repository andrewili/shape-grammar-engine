#   sg_labeled_shape_test.txt

    >>> import sg_line
    >>> import sg_labeled_shape

    ### construct, represent
    >>> def shape_for_init_test():
    ...     line_0001 = sg_line.SGLine(0, 0, 0, 1)
    ...     line_0010 = sg_line.SGLine(0, 0, 1, 0)
    ...     lines = [line_0010, line_0001]
    ...     return lines
    >>> def lp_partition_for_init_test():
    ...     
    >>> lshape = lshape_for_init_test(shape_for_init_test, lp_partition_for_init_test)
    >>> print lshape
    {}